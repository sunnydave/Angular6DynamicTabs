import { TabDirectiveDirective } from './tab-directive.directive';

describe('TabDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new TabDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
